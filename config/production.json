{
	"port": "${ secrets.port }",
	"noindex": true,
	"readonly": false,

	"webservice": {
		"database": "${ secrets.database }",
		"host": "${ secrets.db_host }",
		"port": "${ secrets.db_port }",
		"cron": "0 30 0 * * *"
	}
}